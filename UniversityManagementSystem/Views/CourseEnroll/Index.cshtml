@model IEnumerable<UniversityManagementSystem.Models.CourseEnroll>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course.CourseCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentRegNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnrollDate)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentRegNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnrollDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CourseEnrollId }) |
                @Html.ActionLink("Details", "Details", new { id=item.CourseEnrollId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CourseEnrollId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
